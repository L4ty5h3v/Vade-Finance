{
  "name": "vade-finance-anchor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "test": "ts-mocha -p tsconfig.json -t 1200000 tests/**/*.ts",
    "test:anchor": "anchor test",
    "deploy:devnet": "anchor build && anchor deploy --provider.cluster devnet",
    "bootstrap:devnet": "node scripts/bootstrap-devnet.mjs",
    "mint:devnet": "node scripts/mint-devnet-usdt.mjs"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "0.32.1",
    "@solana/spl-token": "0.4.14",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "chai": "^4.5.0",
    "mocha": "^11.7.5",
    "ts-mocha": "^10.1.0",
    "typescript": "^5.9.3"
  }
}
